---
phase: 01-foundation
plan: 07
type: execute
wave: 4
depends_on:
  - "01-05"
  - "01-06"
files_modified:
  - .env.local
  - .dev.vars
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

user_setup:
  - service: supabase
    why: "Postgres database host — provides DATABASE_URL (pooler, port 6543) and DIRECT_URL (port 5432 for migrations)"
    steps:
      - task: "Create a free Supabase project"
        location: "https://supabase.com → New project"
        notes: "Name it 'rajyash-food-rescue'. Select region closest to Ahmedabad (Mumbai/Singapore). Free tier is sufficient."
      - task: "Obtain DATABASE_URL (pooler)"
        location: "Supabase Dashboard → Project Settings → Database → Connection pooling → Transaction mode → URI"
        notes: "Port must be 6543. URL format: postgres://postgres.PROJECTREF:PASSWORD@aws-REGION.pooler.supabase.com:6543/postgres"
      - task: "Obtain DIRECT_URL"
        location: "Supabase Dashboard → Project Settings → Database → Connection string → URI"
        notes: "Port must be 5432. Used only by drizzle-kit for migrations — never in the app."
  - service: clerk
    why: "Authentication provider — provides CLERK_SECRET_KEY and NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY"
    steps:
      - task: "Create a Clerk application"
        location: "https://clerk.com → Create application"
        notes: "Name it 'Rajyash Food Rescue'. Enable 'Email address' and 'Phone number' sign-in methods."
      - task: "Obtain API keys"
        location: "Clerk Dashboard → API Keys"
        notes: "Copy: Secret key (sk_test_...) → CLERK_SECRET_KEY; Publishable key (pk_test_...) → NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY"
      - task: "Configure custom session token claim"
        location: "Clerk Dashboard → Configure → Sessions → Customize session token"
        notes: |
          Add this JSON to the session token customization:
          { "metadata": "{{user.public_metadata}}" }
          This is REQUIRED for middleware RBAC to work. Without it, sessionClaims.metadata.role is always undefined
          and all authenticated users get redirected to /onboarding forever (Pitfall 5 in RESEARCH.md).
      - task: "Enable phone OTP (verify)"
        location: "Clerk Dashboard → Configure → User & Authentication → Email, Phone, Username"
        notes: "Confirm Phone number is enabled as a sign-in/sign-up method. OTP is handled by Clerk's infra."
  - service: cloudflare
    why: "Deployment target — required for opennextjs-cloudflare deploy command"
    optional: true
    steps:
      - task: "Create a Cloudflare account (if not already)"
        location: "https://dash.cloudflare.com/sign-up"
        notes: "Free tier is sufficient. Workers & Pages free plan includes 100,000 daily requests."
      - task: "Authenticate wrangler CLI"
        location: "Terminal in the project directory"
        notes: "Run: pnpm wrangler login — opens browser for OAuth. Needed for pnpm deploy."

must_haves:
  truths:
    - "New user can sign up at /sign-up (email or phone OTP) and receive a persisted session"
    - "After sign-up, user is redirected to /onboarding; form completes without 401/500"
    - "After onboarding, user can access /portal/dashboard (no redirect back to /onboarding)"
    - "Visiting /portal/dashboard as a non-admin while signed in does NOT return 403"
    - "Visiting an (admin) route (e.g. /admin/dashboard) as a donor returns 403"
    - "Refreshing the browser while signed in preserves the session (no re-login)"
    - "pnpm db:push runs without errors against the real Supabase database"
    - "pnpm run preview starts the CF Workers preview without errors"
  artifacts:
    - path: ".env.local"
      provides: "Real Supabase + Clerk credentials for local next dev"
      contains: "supabase.com"
    - path: ".dev.vars"
      provides: "Real Supabase + Clerk credentials for CF Workers preview (pnpm preview)"
      contains: "CLERK_SECRET_KEY"
  key_links:
    - from: "Clerk Dashboard session token"
      to: "src/middleware.ts sessionClaims?.metadata?.role"
      via: "Custom session claim { metadata: {{user.public_metadata}} }"
      pattern: "Custom session token"
    - from: ".env.local"
      to: "src/config/env.ts"
      via: "process.env values injected at dev startup"
      pattern: "DATABASE_URL"
---

<objective>
[DEFERRED — MANUAL STEPS REQUIRED]

Connect the working code (Plans 01-01 through 01-06) to real external accounts:
Supabase (database), Clerk (auth), and optionally Cloudflare (deploy). Push the database
schema via drizzle-kit. Verify the full auth loop end-to-end.

This plan requires you (the developer) to create accounts and configure the Clerk Dashboard.
Claude cannot do this — these are external services requiring human login.

Purpose: Plans 01-01..06 produce typechecking, linting, and unit-testing code with
placeholder credentials. This plan replaces the placeholders with real credentials and
verifies the entire Phase 1 foundation works end-to-end.

Output: Working auth loop (sign-up → onboarding → portal) on localhost; schema pushed to
Supabase; optional CF Workers preview green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
@.planning/phases/01-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Setup 1: Create Supabase project and obtain database credentials</name>
  <what-built>Plans 01-01..06 created all schema files and the DB client. The drizzle-kit config points to DIRECT_URL for migrations. No Supabase project exists yet.</what-built>
  <how-to-verify>
1. Go to https://supabase.com → sign in → "New project"
2. Name: rajyash-food-rescue. Region: ap-south-1 (Mumbai) — closest to Ahmedabad.
3. After project is ready, go to Project Settings → Database
4. Copy two URLs:
   a. "Connection pooling → Transaction mode → URI" → this is your DATABASE_URL (port 6543)
   b. "Connection string → URI" → this is your DIRECT_URL (port 5432)
5. Both URLs contain your password. Replace [YOUR-PASSWORD] with the actual password you set.
  </how-to-verify>
  <resume-signal>Type the two URLs or confirm "Supabase project created and URLs ready"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Setup 2: Create Clerk application, configure session claim, get API keys</name>
  <what-built>src/app/layout.tsx has ClerkProvider with placeholder keys. The middleware reads sessionClaims.metadata.role. The custom session claim in Clerk Dashboard must be configured or RBAC will never work.</what-built>
  <how-to-verify>
1. Go to https://clerk.com → Create application
   Name: Rajyash Food Rescue
   Enable: Email address + Phone number

2. Go to API Keys → copy:
   - "Secret key" (sk_test_...) → CLERK_SECRET_KEY
   - "Publishable key" (pk_test_...) → NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY

3. CRITICAL: Go to Configure → Sessions → "Customize session token"
   Add this JSON and save:
   ```json
   { "metadata": "{{user.public_metadata}}" }
   ```
   Without this, `sessionClaims?.metadata?.role` is always undefined and RBAC silently fails.

4. Go to Configure → User & Authentication → Email, Phone, Username
   Confirm Phone number is enabled (for AUTH-02 phone OTP).
  </how-to-verify>
  <resume-signal>Type "Clerk configured" after completing all 4 steps (especially the session token claim)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Setup 3: Populate .env.local and .dev.vars with real credentials</name>
  <what-built>Both files exist with placeholder values from Plan 01-01. Replace with real credentials from Steps 1 and 2.</what-built>
  <how-to-verify>
Update `.env.local` (used by `pnpm dev`):
```bash
DATABASE_URL=<your Supabase pooler URL — port 6543>
DIRECT_URL=<your Supabase direct URL — port 5432>
CLERK_SECRET_KEY=<sk_test_...>
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=<pk_test_...>
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/portal/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/onboarding
```

Update `.dev.vars` (used by `pnpm preview` — Cloudflare Workers local preview):
```bash
DATABASE_URL=<same as above>
DIRECT_URL=<same as above>
CLERK_SECRET_KEY=<same as above>
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=<same as above>
```

Both files are gitignored. Never commit real credentials.
  </how-to-verify>
  <resume-signal>Type "credentials populated" after saving both files</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Push database schema to Supabase</name>
  <files>src/server/db/migrations/</files>
  <action>
Run the drizzle-kit schema push against the real Supabase database. This creates the `role`
enum and `profiles` table in the actual Postgres database.

```bash
pnpm db:push
```

This command uses `DIRECT_URL` (port 5432) from `.env.local` — the direct connection,
not the pooler. Drizzle-kit requires DDL on the direct connection.

Expected output: "Changes applied" or similar. If prompted to confirm, type "yes".

If the push fails with "role already exists": the enum was already created — safe to ignore.
If it fails with auth errors: verify DIRECT_URL in .env.local uses port 5432, not 6543.

After push, verify the table exists:
```bash
pnpm dlx drizzle-kit studio
# Opens a browser to https://local.drizzle.studio — verify 'profiles' table appears
# Ctrl+C to exit when done
```
  </action>
  <verify>
    <automated>pnpm db:push 2>&1 | tail -10</automated>
  </verify>
  <done>
    `pnpm db:push` exits 0. `profiles` table exists in Supabase (verify via Supabase Dashboard → Table Editor or drizzle-kit studio).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end auth loop verification</name>
  <what-built>
    All Phase 1 code is wired to real Supabase + Clerk credentials. The full auth loop
    should now work: sign up → onboarding → portal dashboard. Role-based access should
    gate admin routes.
  </what-built>
  <how-to-verify>
Run `pnpm dev` and test this exact flow:

**Test 1 — Public route access:**
- Visit http://localhost:3000 → should see the Rajyash placeholder landing page (warm cream background)
- No redirect, no 401

**Test 2 — Unauthenticated portal redirect:**
- Visit http://localhost:3000/portal/dashboard → should redirect to /sign-in
- URL in browser bar: /sign-in (with return URL param)

**Test 3 — Sign up flow (email):**
- Visit /sign-up → Clerk sign-up form appears (saffron-themed)
- Sign up with a test email
- Verify email (check inbox for Clerk verification email)
- After verification: should redirect to /onboarding

**Test 4 — Onboarding:**
- /onboarding shows the role-select form
- Select "Food Donor" and enter a name
- Submit → should redirect to /portal/dashboard
- Verify: no redirect back to /onboarding on page refresh

**Test 5 — Session persistence (AUTH-06):**
- While on /portal/dashboard, close the browser tab
- Open a new tab, go to http://localhost:3000/portal/dashboard
- Should load dashboard directly (no sign-in prompt)

**Test 6 — Role-based 403:**
- Visit http://localhost:3000/admin/dashboard while signed in as a donor
- Should return a blank page with HTTP status 403 (check browser DevTools → Network tab)
- Should NOT redirect to sign-in (explicit 403 is correct)

**Test 7 — Phone OTP (AUTH-02):**
- Sign out (visit /sign-in, click "Sign out" or clear Clerk cookies)
- Go to /sign-up → choose "Continue with phone"
- Enter a real phone number and complete OTP verification
- Complete onboarding → verify phone user can access /portal/dashboard

If any test fails, describe the failure and I will diagnose.
  </how-to-verify>
  <resume-signal>Type "all 7 tests pass" or describe which test failed and the error shown</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="optional">
  <name>Checkpoint (Optional): Cloudflare Workers preview</name>
  <what-built>
    The app is fully functional on `pnpm dev` (Node.js). This optional checkpoint verifies
    the Cloudflare Workers adapter builds and runs the app correctly in the CF runtime.
    Required before deploying to production Cloudflare Pages.
  </what-built>
  <how-to-verify>
If you have a Cloudflare account and have run `pnpm wrangler login`:

```bash
pnpm preview
```

This runs `opennextjs-cloudflare build && opennextjs-cloudflare preview`, which:
1. Builds the Next.js app
2. Wraps it with the CF Workers adapter
3. Starts a local CF Workers preview on http://localhost:8787

Visit http://localhost:8787 and repeat the sign-up → onboarding → portal flow.

Common issues:
- "Worker size exceeds 3 MiB" → run `pnpm wrangler build --analyze` to find large imports
- "FinalizationRegistry not found" → verify `compatibility_date: "2025-05-05"` in wrangler.jsonc
- Auth doesn't work in preview → verify .dev.vars has real Clerk keys (not placeholders)

This checkpoint is OPTIONAL for Phase 1. The auth loop is verified in Node.js mode above.
  </how-to-verify>
  <resume-signal>Type "CF preview works" or "skipping for now" if you don't have a Cloudflare account yet</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local / .dev.vars → developer machine | Real secrets; must never leave the machine unencrypted |
| Supabase dashboard → drizzle-kit | Direct DB connection (port 5432) used only during setup |
| Clerk Dashboard → session token config | The metadata claim is a one-time manual config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Real credentials in .env.local | mitigate | File is gitignored; .env.example and .dev.vars.example committed instead. Developer must never paste real keys into example files |
| T-07-02 | Information Disclosure | Real credentials in .dev.vars | mitigate | File is gitignored. Verified in Plan 01-01 task 1 |
| T-07-03 | Spoofing | Missing Clerk session claim causes silent RBAC failure | mitigate | Checkpoint:human-action explicitly requires the session claim step. The setup note calls it CRITICAL. Without it, role is always undefined and all routes are inaccessible (FORBIDDEN) or redirect-loop |
| T-07-04 | Tampering | drizzle-kit push on wrong database | accept | Developer controls the DIRECT_URL; documented to use the Supabase project URL at port 5432 |
</threat_model>

<verification>
Full suite must remain green after credentials are populated:

```bash
pnpm test:run                # 16 tests green
pnpm typecheck               # 0 errors
pnpm lint                    # 0 errors
```

Phase 1 functional gate (manual):
- [ ] Sign up (email) → onboarding → /portal/dashboard (no re-redirect)
- [ ] Session persists across browser tab close + reopen
- [ ] /admin/dashboard as donor → HTTP 403 (not 302 redirect)
- [ ] Phone OTP sign-up → onboarding → dashboard
- [ ] `pnpm db:push` → profiles table created in Supabase
</verification>

<success_criteria>
- Real Supabase + Clerk credentials in .env.local and .dev.vars (not placeholders)
- `pnpm db:push` exits 0; profiles table + role enum exist in Supabase
- Clerk session claim `{ "metadata": "{{user.public_metadata}}" }` is configured in dashboard
- All 7 manual test flows pass on localhost
- All 16 automated tests still pass
- `pnpm typecheck` + `pnpm lint` exit 0
- (Optional) `pnpm preview` exits 0 and serves the app in CF Workers runtime
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
